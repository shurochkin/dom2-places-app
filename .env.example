# Stadia Maps tile API key. Provision at https://client.stadiamaps.com,
# restrict it to your domain(s) (Stadia dashboard → Authorized Domains),
# then set this locally and as a GitHub Actions secret named
# STADIA_API_KEY for CI builds.
PUBLIC_STADIA_API_KEY=
